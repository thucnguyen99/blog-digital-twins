import type { Entry } from '../../types'
import type { OwnerProfile } from '../../types'

// Phase 2 stub — full dot-skill SKILL.md aggregation not yet implemented
export function generateDotSkillExport(
  _entries: Entry[],
  _profile: OwnerProfile,
): string {
  return '# SKILL.md\n\n// Phase 2: dot-skill export not yet implemented\n'
}
